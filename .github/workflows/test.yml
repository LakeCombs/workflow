name: Github Action For React
on: [push]
jobs:
  build-app:
    runs-on: ubuntu-latest
    steps:
      - run: echo "hello world!!"
      - run: echo "this is step number 2"
  print-hello workd:
    runs-on: ubuntu-latest
    steps:
      - run: echo "hello world again!!"
      - run: echo "this is step number 2 the ssecond time"
